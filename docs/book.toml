[book]
authors = ["Tony Wu"]
language = "en"
multilingual = false
src = "src"
title = "mdbookkit"

[build]
build-dir = "dist"
create-missing = false
extra-watch-dirs = ["app"]

[rust]
edition = "2021"

[output.html]
additional-css = ["build/index.css"]
default-theme = "ayu"
git-repository-icon = "fa-github"
git-repository-url = "https://github.com/tonywu6/mdbookkit"
hash-files = true
preferred-dark-theme = "ayu"
site-url = "/mdbookkit/"

[output.html.playground]
runnable = false

[preprocessor.rustdoc-link]
after = ["links"]
command = "cargo run --release --package mdbook-rustdoc-link"

cache-dir = "build"
manifest-dir = "../crates/rustdoc-link"
rust-analyzer = "cargo run --release --package util-rust-analyzer -- analyzer"

[preprocessor.alerts]
after = ["rustdoc-link"]

[preprocessor.ra-version]
after = ["rustdoc-link"]
command = "cargo run --release --package util-rust-analyzer -- version"

[preprocessor.app]
command = "deno run -A app/build/preprocessor.ts"
