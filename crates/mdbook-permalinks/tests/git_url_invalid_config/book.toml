[preprocessor.permalinks]
repo-url-template = [
  "https://git.example.org/lorem/ipsum/{tree}/{kind}/{ref}/{path}",
  [
    [
    ],
  ],
]

[output.markdown]

[build]
build-dir = "dist"
create-missing = false
use-default-preprocessors = true
