[preprocessor.permalinks]
repo-url-template = "https://git.example.org/{tree}/{ref}/{path}"

[output.markdown]

[output.html]
site-url = "https://book.example.org"

[build]
build-dir = "dist"
create-missing = false
use-default-preprocessors = true
