[preprocessor.rustdoc-links]
after = ["links"]
command = "cargo preprocessor"

[preprocessor.rustdoc-links.build]
rustc-args = ["--check-cfg", "cfg(fancy)", "--cfg", "fancy"]

[build]
build-dir = "dist"
create-missing = false

[output.markdown]
