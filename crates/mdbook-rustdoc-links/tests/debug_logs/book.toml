[[preprocessor.rustdoc-links.build]]
targets = ["aarch64-unknown-linux-gnu"]

[[preprocessor.rustdoc-links.build]]
features = ["fancy"]
packages = [{ workspace = true, dependencies = false }]
runner = "bash -c $*"

[output.markdown]

[build]
build-dir = "dist"
create-missing = false
use-default-preprocessors = true
