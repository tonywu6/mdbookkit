[preprocessor.rustdoc-links]
after = ["links"]
command = "cargo preprocessor"

[preprocessor.rustdoc-links.build]
features = ["anstyle-parse/utf8"]
packages = ["anstyle-parse", { workspace = true }]

[build]
build-dir = "dist"
create-missing = false

[output.markdown]
