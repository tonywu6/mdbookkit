[preprocessor.rustdoc-links.build]
preludes = ['std; compile_error!("sup")']
runner = "bash bin/runner.sh $@"

[output.markdown]

[build]
build-dir = "dist"
create-missing = false
use-default-preprocessors = true
