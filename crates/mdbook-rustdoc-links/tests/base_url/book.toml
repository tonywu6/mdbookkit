[preprocessor.rustdoc-links]
after = ["links"]
build.packages = [{ workspace = true, dependencies = true }]
command = "cargo preprocessor"

base-url.release = "https://example.org/api"

[build]
build-dir = "dist"
create-missing = false

[output.markdown]
